// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: quota.sql

package gen

import (
	"context"
	"database/sql"
	"time"
)

const listLatestQuotaSnapshots = `-- name: ListLatestQuotaSnapshots :many
SELECT q.id, q.harness, q.account_id, q.model, q.window_start, q.window_end, q.used, q.remaining, q.limit_value, q.signal_quality, q.source, q.basis, q.observed_at
FROM quota_snapshots q
JOIN (
    SELECT harness, account_id, model, max(observed_at) AS observed_at
    FROM quota_snapshots
    GROUP BY harness, account_id, model
) latest
  ON latest.harness = q.harness
 AND latest.account_id = q.account_id
 AND latest.model = q.model
 AND latest.observed_at = q.observed_at
ORDER BY q.harness ASC, q.account_id ASC, q.model ASC
`

func (q *Queries) ListLatestQuotaSnapshots(ctx context.Context) ([]QuotaSnapshot, error) {
	rows, err := q.db.QueryContext(ctx, listLatestQuotaSnapshots)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []QuotaSnapshot{}
	for rows.Next() {
		var i QuotaSnapshot
		if err := rows.Scan(
			&i.ID,
			&i.Harness,
			&i.AccountID,
			&i.Model,
			&i.WindowStart,
			&i.WindowEnd,
			&i.Used,
			&i.Remaining,
			&i.LimitValue,
			&i.SignalQuality,
			&i.Source,
			&i.Basis,
			&i.ObservedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertQuotaSnapshot = `-- name: UpsertQuotaSnapshot :one
INSERT INTO quota_snapshots (
    id, harness, account_id, model, window_start, window_end,
    used, remaining, limit_value, signal_quality, source, basis, observed_at
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
ON CONFLICT(harness, account_id, model, window_start, window_end) DO UPDATE SET
    used = excluded.used,
    remaining = excluded.remaining,
    limit_value = excluded.limit_value,
    signal_quality = excluded.signal_quality,
    source = excluded.source,
    basis = excluded.basis,
    observed_at = excluded.observed_at
RETURNING id, harness, account_id, model, window_start, window_end, used, remaining, limit_value, signal_quality, source, basis, observed_at
`

type UpsertQuotaSnapshotParams struct {
	ID            string
	Harness       string
	AccountID     string
	Model         string
	WindowStart   sql.NullTime
	WindowEnd     sql.NullTime
	Used          sql.NullFloat64
	Remaining     sql.NullFloat64
	LimitValue    sql.NullFloat64
	SignalQuality string
	Source        string
	Basis         string
	ObservedAt    time.Time
}

func (q *Queries) UpsertQuotaSnapshot(ctx context.Context, arg UpsertQuotaSnapshotParams) (QuotaSnapshot, error) {
	row := q.db.QueryRowContext(ctx, upsertQuotaSnapshot,
		arg.ID,
		arg.Harness,
		arg.AccountID,
		arg.Model,
		arg.WindowStart,
		arg.WindowEnd,
		arg.Used,
		arg.Remaining,
		arg.LimitValue,
		arg.SignalQuality,
		arg.Source,
		arg.Basis,
		arg.ObservedAt,
	)
	var i QuotaSnapshot
	err := row.Scan(
		&i.ID,
		&i.Harness,
		&i.AccountID,
		&i.Model,
		&i.WindowStart,
		&i.WindowEnd,
		&i.Used,
		&i.Remaining,
		&i.LimitValue,
		&i.SignalQuality,
		&i.Source,
		&i.Basis,
		&i.ObservedAt,
	)
	return i, err
}
