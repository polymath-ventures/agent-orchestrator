# aong shutdown

Stop all live work first, then stop the daemon. If work cannot be stopped, the
daemon is left running so live agents are not stranded without supervision.

## Syntax

```
aong shutdown
```

## Examples

```bash
aong shutdown
```
