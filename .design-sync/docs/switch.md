---
category: Forms
---

# Switch

Boolean toggle. Use for settings that apply immediately; use a checkbox pattern
for values that need an explicit save.

```tsx
<div className="flex items-center gap-2">
	<Switch id="auto-merge" defaultChecked />
	<Label htmlFor="auto-merge">Auto-merge when green</Label>
</div>
```
