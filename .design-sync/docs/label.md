---
category: Forms
---

# Label

Form label bound to a control via `htmlFor`. Always label an `Input`, `Select`
or `Switch` — none of them carry their own text.

```tsx
<Label htmlFor="model">Model</Label>
<Input id="model" />
```
