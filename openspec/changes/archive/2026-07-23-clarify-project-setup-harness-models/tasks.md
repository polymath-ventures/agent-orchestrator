## 1. First-Run Setup

- [x] 1.1 Add focused tests for worker/orchestrator/reviewer harness labels,
      automatic reviewer wording, selected-harness model rows, manual model
      notice, and create-sheet submit payload.
- [x] 1.2 Add focused tests for `createProjectConfig` persistence of reviewer
      harness and selected-harness model defaults.
- [x] 1.3 Update `CreateProjectAgentSheet` to render role harness selectors and
      selected-harness default model rows.
- [x] 1.4 Update project creation payload construction to emit existing
      `ProjectConfig` fields only for selected harnesses with configured values.
- [x] 1.5 Update create-flow integration expectations and run focused frontend
      tests plus OpenSpec validation.
